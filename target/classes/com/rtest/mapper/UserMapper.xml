<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	<mapper namespace="com.rtest.mapper.UserMapper">

<!-- 로그인  -->
<select id="login" resultType="com.rtest.domain.UserVO">
	select user_id , user_passwd , user_name from user_test where user_id = #{user_id} and user_passwd = #{user_passwd}
</select>


</mapper>

